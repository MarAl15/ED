//
//  Conjunto_Letras.cpp
//  
//
//
//

#include "Conjunto_Letras.h"
#include "Letras.h"
#include<iostream>
#include<map>
#include<vector>
#include<string>
using namespace std;

istream & operator >>(istream &f, Conjunto_Letras &c){
    Letras nueva;
    string aux;
	int tamanio;
    vector<string> n;
    

	int j = 1 ;
    if(!f.eof())
       getline(f,aux);
    
    char l;
    int c,p;
    while(!f.eof()){
        cin>>l;
        cin>>c;
        cin>>p;
        
        nueva.asignaLetra(l);
        nueva.asignaNum(c);
        nueva.asignaPuntos(p);
        
        for(int i = 0 ; i < c; i++){
            c.letras.insert(nueva);
        }
    }
/*
		tamanio = aux.size();
        nueva.asignaLetra(aux.at(0));

        for( int i = 2 ; i < tamanio && aux.at(i) != '\t'; i++){
            n.at(0).push_back(aux.at(i));
			contador++;
        }
        
		for(int j = contador ; j < tamanio ; j++){
			n.at(1).push_back(aux.at(j));
		}
int a;
		a = stoi(n[0],nullptr,10);
        nueva.asignaNum(a);
       // nueva.asignaPuntos(n[1]);
        
		
		aux.clear();
		for( int i = 0 ; i < n[0] ; i++)
       		c.letras.insert(nueva);
 */
    
}

map<char,int> Conjunto_Letras::devuelveVocales(){
	

	
}

map<char,int> Conjunto_Letras::devuelveConsonantes(){

}





